## Setup

Make sure to install dependencies:

```bash
# npm
npm install
```

## Development Server

Make sure to create a .env file by making a copy of .env.example and set your private keys.

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev
```
